# snake-zig

A console Snake game written in Zig using libvaxis for terminal UI.

## Requirements

- Zig 0.15.2

## Build

```bash
zig build
```

## Run

```bash
zig build run
```

Press `q` or `Ctrl+C` to quit.

## Test

```bash
zig build test
```
